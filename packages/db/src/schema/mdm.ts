/**
 * OpenMDM Drizzle Schema for PostgreSQL
 *
 * Re-exported from @openmdm/drizzle-adapter/postgres — the canonical schema
 * maintained by the library. This keeps the demo's database in lockstep with
 * the adapter across releases instead of hand-maintaining a copy that drifts.
 *
 * Includes: devices, policies (+ versions), applications, commands, events,
 * groups, device groups, push tokens, app deployments, app versions,
 * rollbacks, device app inventory, plugin storage, enrollment challenges.
 */

export * from "@openmdm/drizzle-adapter/postgres";
